name: Version Consistency Check
on:
  push:
    paths:
      - 'package.json'
      - '.github/workflows/version-check.yml'
    branches:
      - 'main'
      - 'pre-release'
      - 'develop'
  pull_request:
    types:
      - reopened
      - opened
      - synchronize
    paths:
      - 'package.json'
      - '.github/workflows/version-check.yml'

permissions:
  contents: read

jobs:
  test:
    name: Version Consistency Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - id: package-json
        uses: mikefarah/yq@v4.45.1
        with:
          cmd: yq -oy '.version' package.json
      - name: Check Version Exists
        run: |
          echo "Package.json version: ${{ steps.package-json.outputs.result }}"
          if [[ -z "${{ steps.package-json.outputs.result }}" ]]; then
            echo "Error: package.json version is missing. Please check package.json."
            exit 1
          fi
          echo "Version check passed."
